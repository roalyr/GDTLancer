## CURRENT GOAL: Jump Transition Visual Cover and Particle Isolation
- TARGET_FILE: src/scenes/game_world/world_manager.gd
- TRUTH_RELIANCE: TRUTH_CONSTRAINTS.md §1; TRUTH_CONTENT-CREATION-MANUAL.md §2 `/scenes` and `/assets`, §5.3, §6.1, §6.3, §7; TRUTH_DOCS_CanvasItem_Godot_3.6.md §Render modes and §Global built-ins; TRUTH_DOCS_Particle shaders_Godot_3.6.md note plus §Render modes; TRUTH_SIMULATION-GRAPH.md §1 as a boundary that this milestone is cosmetic-only and must not alter simulation or sector-travel semantics.
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [x] TASK_1: Add jump-transition-owned visual masking and dedicated transition-only particle scaffolding without changing the current route-based jump flow. Required signatures: preserve `world_manager.gd::func travel_to_sector(target_sector_id: String) -> void`, `world_manager.gd::func _run_jump_transition_sequence(target_sector_id: String) -> void`, `world_manager.gd::func _prepare_jump_transition_departure_visuals(departure_direction: Vector3)`, `jump_transition_rig.gd::func begin_departure(source_sector_id: String, target_sector_id: String, travel_direction: Vector3) -> void`, `jump_transition_rig.gd::func begin_cruise(target_velocity: float) -> void`, `jump_transition_rig.gd::func begin_arrival() -> void`, and `jump_transition_rig.gd::func reset_transition_state() -> void`. Required scene contract: `scenes/prefabs/navigation/jump_transition_rig.tscn` must expose a CanvasItem-based fullscreen `TransitionOverlay` for departure/arrival cover, a dedicated `JumpTransitionParticles` container for jump-only emitters, and a reset path that returns both systems to a fully cleared idle state; all transition particles must remain GLES2-safe by using `CPUParticles`, not particle shaders.
  - [x] TASK_2: Separate gameplay-camera particles from transition particles and wire explicit clear/pause/resume control through the jump sequence. Required signatures: preserve `orbit_camera.gd::func restore_orbit_from_transition_view(target: Spatial, forward_direction: Vector3) -> void`, `orbit_camera.gd::func set_temporary_fov_override(fov_deg: float) -> void`, `orbit_camera.gd::func clear_temporary_fov_override() -> void`, and `world_manager.gd::func _restore_gameplay_camera_at_transition_fov() -> void`; add stable runtime hooks `orbit_camera.gd::func set_local_scene_particles_active(is_active: bool, clear_existing: bool = false) -> void` and `jump_transition_rig.gd::func set_transition_particles_active(is_active: bool, clear_existing: bool = false) -> void`. Required behavior: regroup the authored OrbitCamera dust emitters under `LocalSceneParticles`, disable and clear that group before local-scene despawn, keep it paused until arrival completes, activate only `JumpTransitionParticles` during the transition window, and guarantee that neither particle group bleeds residual particles across despawn/load or from one jump into the next.
  - [x] VERIFICATION: Confirm focused diagnostics on `src/scenes/game_world/world_manager.gd`, `src/scenes/game_world/jump_transition/jump_transition_rig.gd`, `src/scenes/camera/orbit_camera.gd`, `src/scenes/camera/components/camera_particles_controller.gd`, `scenes/prefabs/camera/orbit_camera.tscn`, and `scenes/prefabs/navigation/jump_transition_rig.tscn`; confirm the focused jump slices for `src/tests/scenes/game_world/world_manager/test_world_manager.gd`, `src/tests/scenes/jump_transition/test_jump_transition_regressions.gd`, and `src/tests/scenes/camera/test_orbit_camera.gd`, plus a focused particle-control regression if new runtime hooks are introduced; confirm `godot --no-window --quit` is green. Success criteria: departure and arrival each render a visible masking pass over the moment scene content swaps, gameplay-camera particles are fully cleared and invisible throughout the transition, jump-only particles render only inside the jump rig window, and a second consecutive jump starts from a clean particle state with no residual carry-over.
