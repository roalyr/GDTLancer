## CURRENT GOAL: Debug Map Readability And Control Surface
- TARGET_FILE: src/core/ui/debug_map_panel/debug_map_panel.gd
- TRUTH_RELIANCE: TRUTH_CONTENT-CREATION-MANUAL.md §2 Directory Structure Reference; TRUTH_CONTENT-CREATION-MANUAL.md §5.4 UI Assets; TRUTH_CONTENT-CREATION-MANUAL.md §6.3 GUT Unit Tests; TRUTH_SIMULATION-GRAPH.md § Sector Discovery (The Counter-Force to Heat Death); TRUTH_SIMULATION-GRAPH.md §6.4 System Interaction Matrix; TRUTH_CONSTRAINTS.md §1
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [x] TASK_1: Extend the debug map readability layer so labels, connection lines, and sector node icons can each be toggled independently from the header row while preserving authored-vs-discovered styling semantics. Required signatures: preserve debug_map_panel.gd::func _populate_map(), debug_map_panel.gd::func _create_sector_markers(), debug_map_panel.gd::func _create_connection_lines(), debug_map_panel.gd::func _update_label_positions(), debug_map_panel.gd::func _connect_buttons(), and main_hud.gd::func _compute_projected_target_distance_fade_alpha(normalized_distance: float) -> float; add only narrow helper methods for debug-map visibility state and map-label fade-alpha calculation. Required behavior: add dedicated header buttons for labels, lines, and icons; toggles must apply immediately, survive in-panel `_populate_map()` refreshes triggered by sim ticks, and preserve current discovered-sector prefixes/colors when visible; projected map labels must fade by distance from the map viewport center using the same edge-alpha / curve mechanics already used by MainHUD projected target overlays.
  - [x] TASK_2: Extend debug map interaction/render controls to improve navigation readability without changing the existing rendering scope. Required signatures: preserve debug_map_panel.gd::func _input(event), debug_map_panel.gd::func _handle_map_mouse_input(event), debug_map_panel.gd::func _update_camera(), debug_map_panel.gd::func _zoom(factor: float), debug_map_panel.gd::func _pan(direction: Vector3), and debug_map_panel.gd::func _toggle_panel(); add only narrow helper methods for right-drag pan state, FoV stepping, and viewport MSAA cycling. Required behavior: keep left mouse drag as orbit rotation, add right mouse drag to pan the map in camera space, keep mouse-wheel zoom on orbit distance, repurpose or supplement header controls so map camera FoV has explicit `+` / `-` buttons with safe clamping, and add a header button that cycles the map viewport anti-aliasing mode through `off`, `2x`, and `4x` only; do not introduce nebula changes, renderer/backend changes, or shader-only AA experiments.
  - [x] VERIFICATION: Add focused deterministic coverage in test_debug_map_panel.gd and test_debug_map_panel_focus.gd; confirm touched-file diagnostics are clean; run a focused GUT slice with `-gdir=` override; confirm godot --no-window --quit is green. Success criteria: the panel can independently hide labels, lines, and node icons; projected label alpha falls off toward the map viewport edges with the same curve semantics as MainHUD; right-drag panning works without breaking left-drag orbit or wheel zoom; FoV buttons change `MapCamera.fov`; the AA button cycles `Viewport.msaa` through disabled, 2x, and 4x with visible state feedback; and authored/discovered sector readability remains distinct after map refreshes.
