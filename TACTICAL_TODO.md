## CURRENT GOAL: Dedicated Cosmetic Jump Transition
- TARGET_FILE: src/scenes/game_world/world_manager.gd
- TRUTH_RELIANCE: TRUTH_PROJECT.md (platform and graphics constraints); TRUTH_CONSTRAINTS.md §1; TRUTH_CONTENT-CREATION-MANUAL.md §2 and §6.1/§6.3; TRUTH_SIMULATION-GRAPH.md §3.2 and §3.3 as simulation-boundary references; TRUTH_DOCS_Particle shaders_Godot_3.6.md note that particle shaders are GLES3-only, so this milestone must avoid GPU particle-shader dependencies.
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [ ] TASK_1: Create a dedicated pause-capable jump transition rig and internal runtime toggle without changing the current travel API or simulation semantics. Required signatures: preserve `world_manager.gd::func travel_to_sector(target_sector_id: String) -> void` and `world_manager.gd::func load_sector(sector_id: String)`; add `world_manager.gd::func is_jump_transition_active() -> bool`; add `world_rendering.gd::var jump_transition_enabled = true`; add a dedicated jump-transition rig script with explicit departure, cruise, and reset entry points plus `Node.PAUSE_MODE_PROCESS` behavior. The rig must render only the global nebula content during cruise.
  - [ ] TASK_2: Integrate the rig into the live sector-jump flow. Required behavior: lock camera rotation and input immediately, hide MainHUD after a short delay, widen camera FoV toward about 170, unload the current zone and agents before cruise, accelerate to a fixed travel speed in the nebula rig, decelerate near arrival, load the destination sector only after departure cleanup is complete, restore the gameplay camera at widened FoV, then return FoV to its standard gameplay value, unlock controls, and show MainHUD after a short delay. Required signatures: preserve `player_controller_ship.gd::_handle_interact_input() -> void`, `main_hud.gd::_on_sector_changed(_new_sector_id, _old_sector_id) -> void`, and `orbit_camera.gd::func get_camera_mode() -> int`; add a narrow orbit-camera temporary FoV override API so the jump sequence does not fight the normal zoom controller.
  - [ ] VERIFICATION: Add focused GUT coverage for world-manager jump-transition gating, disabled-toggle instant fallback, active-state cleanup after arrival, and any new orbit-camera FoV override path. Run the narrow world-manager and docking-jump slices plus `godot --no-window --quit`. Manual Godot pass: repeated A→B→A jumps, HUD hide and show delays, paused gameplay during cruise, no stray local-scene nodes during nebula travel, restored controls on arrival, and unchanged current instant travel when the toggle is off.
