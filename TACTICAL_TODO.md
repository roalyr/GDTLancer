## CURRENT GOAL: Simulation-Driven Frontier Sector Materialization
- TARGET_FILE: src/core/simulation/agent_layer.gd
- TRUTH_RELIANCE: TRUTH_SIMULATION-GRAPH.md § Sector Discovery (The Counter-Force to Heat Death); TRUTH_SIMULATION-GRAPH.md § 6.4 System Interaction Matrix
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [x] TASK_1: Extend agent_layer.gd::func _try_exploration(agent_id: String, agent: Dictionary, sector_id: String) -> void so each successful discovery also creates a runtime procedural LocationTemplate in TemplateDatabase.locations with deterministic global_position, location_name, sector_type, connections, is_procedural, procedural_type, procedural_hints, and sector_description. Source all discovery spatial-branching controls from Constants.gd. Required signatures: preserve agent_layer.gd::func _try_exploration(agent_id: String, agent: Dictionary, sector_id: String) -> void, agent_layer.gd::func _generate_sector_name() -> String, agent_layer.gd::func _nearby_candidates(source_id: String, exclude: Array) -> Array, and agent_layer.gd::func _distant_loop_candidate(source_id: String, exclude: Array); add only narrow helper methods for discovered-sector position generation, low-visibility type selection, and runtime template registration as needed.
  - [x] TASK_2: Make runtime-discovered sectors visible and traversable in the live spatial universe without handcrafted content. Required signatures: preserve debug_map_panel.gd::func _populate_map(), debug_map_panel.gd::func _create_sector_markers(), debug_map_panel.gd::func _create_connection_lines(), debug_map_panel.gd::func _on_sim_tick_completed(_tick_count), sim_debug_panel.gd::func _on_run_batch(tick_count: int) -> void, sim_debug_panel.gd::func _refresh() -> void, sector_loader.gd::func load_sector(sector_id: String) -> Spatial, and route_target_provider.gd::func build_targets_for_sector(source_sector_id: String) -> Array. Required behavior: discovered sectors use the existing generic procedural fallback scene only, no nebula changes are introduced, discovered markers and route lines are visually distinct from handcrafted sectors on the debug map, running tick batches in the Sim Debug Panel exposes the discovery results, and newly discovered sectors can be reached through generated jump routes with source-to-discovery spacing shorter than normal system-to-system spacing and loop links allowed only when spatial proximity remains plausible.
  - [x] VERIFICATION: Add focused deterministic coverage in test_agent_layer.gd, test_route_target_provider.gd, test_sector_loader.gd, and test_debug_map_panel.gd; confirm touched-file diagnostics are clean; run focused GUT slices with -gdir overrides; confirm godot --no-window --quit is green. Success criteria: after a debug-panel batch run, discovered sectors appear on the debug map with distinct authored-vs-discovered visuals, generated jump routes point to them correctly, traveling to a discovered sector loads the generic fallback scene with valid outbound jump points, and no handcrafted sector visuals or nebula positioning regress.
