## CURRENT GOAL: HUD Toggle State Integration and Bracket Fade
- TARGET_FILE: src/core/ui/main_hud/main_hud.gd
- TRUTH_RELIANCE: TRUTH_PROJECT.md (Project stack: Godot3/GLES2/Python3); TRUTH_CONSTRAINTS.md §1; TRUTH_CONTENT-CREATION-MANUAL.md §2 "/assets" and "/scenes" directory layout, §5.4 "UI Assets", §6.1 "Quick Validation", §6.3 "GUT Unit Tests"; SESSION-LOG.md entries 2026-05-14 02:51:14 through 2026-05-14 03:05:12
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [x] TASK_1: Expose persistent HUD-readable state from the live piloting owners before any button-highlighting work. Preserve the current command-entry flow and add read-only accessors only. Required signatures: `player_controller_ship.gd::func is_free_flight_active() -> bool`, `player_controller_ship.gd::func get_active_navigation_command_type() -> int`, `navigation_system.gd::func get_current_command_type() -> int`; reuse `orbit_camera.gd::func get_camera_mode() -> int` unchanged. `MainHUD` may call these accessors but must not inspect `_current_input_state` or `_current_command` directly after this task.
  - [x] TASK_2: Implement HUD toggle ownership, ON-state visuals, and projected-bracket center fade in the existing HUD/render path. Preserve `MainHUD._rebuild_projected_target_overlays()`, `MainHUD._update_route_target_overlay()`, `MainHUD._update_world_target_overlay()`, `ProjectedTargetBracket.configure_target(new_target_ref, new_target_label: String = "")`, `ProjectedTargetBracket.set_selected_state(is_selected: bool)`, and `orbit_camera.gd` camera-mode API. Required behavior: literal overlay toggles for `ButtonOverlayStructures`, `ButtonOverlayStellar`, and `ButtonOverlayJump`; persistent ON visuals for `ButtonManualFlight`, `ButtonOrbit`, `ButtonApproach`, `ButtonFlee`, and `ButtonCamera`; a projected-bracket distance-fade toggle variable in `world_rendering.gd`; bracket opacity ranging from about `0.10` at viewport edges to `1.00` near center.
  - [X] VERIFICATION: Add focused GUT coverage for state accessors, overlay classification/filtering, button ON-state reflection, and center-vs-edge bracket opacity in `test_main_hud_projected_targeting.gd` plus any narrow touched-slice tests. Run touched-file diagnostics, `godot --no-window -s addons/gut/gut_cmdln.gd -gdir=res://src/tests/core/ui/ -ginclude_subdirs=false -gexit`, and `godot --no-window --quit`. Manual Godot pass: overlay buttons independently filter Jump/Stellar/Structures, persistent buttons track live state, `ButtonCamera` reflects target-follow mode, and bracket fade remains correct after resize.
