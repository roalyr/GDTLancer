<!--
PROJECT: GDTLancer
MODULE: TACTICAL_TODO.md
STATUS: [Level 2 - Implementation]
TRUTH_LINK: TRUTH_PROJECT.md § Project Stack and Context; TRUTH_SIMULATION-GRAPH.md §0, §2.3; TRUTH_CONTENT-CREATION-MANUAL.md §4.1, §4.3; TRUTH-GDD-COMBINED-TEXT-MAJOR-CHANGE-frozen-2026.02.13.md §3, §3.1, §5, §8; MODEL-CASCADE-PROTOCOL.md ROLE: Lead Systems Architect
LOG_REF: 2026-05-24 19:24:50
-->

## CURRENT GOAL: Exploration Productivity And Frontier Topology Calibration
- TARGET_SCOPE: Treat the new composite chronicle as the fixed inspection surface and tune the live qualitative frontier loop so exploration no longer collapses into `expedition_failed` plus `move` dominance, frontier discovery expands more legibly instead of exploding into bottleneck chains, and discovered sectors retain differentiated frontier behavior instead of rapidly homogenizing into generic secure hubs. The recorded composite log now shows the controlling failure clearly: at 3000 ticks the world reaches 204 sectors with 132 bottlenecks while `persistent_nova` logs 2970 `expedition_failed` and 2783 `move` events against only 30 discoveries. Scope covers exploration gating, mortal explorer pressure, discovery topology generation, and frontier progression thresholds. It does not widen chronicle/report UI again, broadly retune non-frontier role semantics, or reintroduce numeric economy models.
- TARGET_FILES:
  - src/autoload/Constants.gd — central owner for exploration chance/cooldown, sector cap, frontier connection odds, mortal spawn pressure, and frontier progression thresholds.
  - src/core/simulation/agent_layer.gd — primary owner for explorer interaction priority, exploration failure reasons, discovery topology generation, and mortal explorer pressure.
  - src/core/simulation/grid_layer.gd — owner for frontier sector security/economy/colony progression so discovered sectors keep legible frontier identity longer.
  - src/tests/core/simulation/test_agent_layer.gd — focused exploration, discovery-topology, and mortal explorer guardrails.
  - src/tests/core/simulation/test_grid_layer.gd — focused frontier progression and stabilization guardrails for tuned thresholds.
  - src/tests/core/simulation/test_simulation_reasonableness.gd — seeded long-run qualitative guardrails extended with composite-log frontier findings.
  - src/tests/core/simulation/test_simulation_integration.gd — seeded long-run integration checks for discovery count, topology shape, and non-collapsed frontier sectors.
- TRUTH_RELIANCE: ["TRUTH_PROJECT.md § Project Stack and Context", "TRUTH_SIMULATION-GRAPH.md §0 Implementation Reality", "TRUTH_SIMULATION-GRAPH.md §2.3 The Agent Nodes", "TRUTH_CONTENT-CREATION-MANUAL.md §4.1", "TRUTH_CONTENT-CREATION-MANUAL.md §4.3", "TRUTH-GDD-COMBINED-TEXT-MAJOR-CHANGE-frozen-2026.02.13.md §3", "TRUTH-GDD-COMBINED-TEXT-MAJOR-CHANGE-frozen-2026.02.13.md §3.1", "TRUTH-GDD-COMBINED-TEXT-MAJOR-CHANGE-frozen-2026.02.13.md §5", "TRUTH-GDD-COMBINED-TEXT-MAJOR-CHANGE-frozen-2026.02.13.md §8"]
- TECHNICAL_CONSTRAINTS: ["Platform (primary): Godot3 (3.6 stable)", "Graphics: GLES2 (for performance and compatibility)", "Additional: Python3 (for sandbox)"]
- ATOMIC_TASKS:
  - [ ] TASK_1: Add deterministic composite-driven frontier guardrails. Required signatures: preserve `SimulationEngine.initialize_simulation(seed_string: String) -> void`, `SimulationEngine.process_tick() -> void`, and the current focused/composite chronicle APIs untouched; extend the existing simulation-side guardrails or add a narrow adjacent harness that validates seeded 300-3000 tick runs for bounded discovery count, bounded topology bottlenecks/degree distribution, and explorer action mixes that do not remain dominated by `expedition_failed` plus `move` at the Nova-like proportions seen in the recorded composite log.
  - [ ] TASK_2: Tune exploration productivity and mortal explorer pressure in the live qualitative loop. Required signatures: preserve `AgentLayer.process_tick(config: Dictionary) -> void` and the chronicle event packet shape `{tick, actor_id, action, sector_id, metadata}`; adjust only exploration success/failure heuristics, broke/cooldown handling, explorer movement bias, mortal-role spawn pressure, and frontier discovery connection selection within the declared frontier owners.
  - [ ] TASK_3: Rebalance frontier progression so discovered sectors retain legible frontier identity and the graph no longer explodes into repetitive bottleneck chains. Required signatures: preserve `GridLayer.process_tick(config: Dictionary) -> void`, the qualitative sector-tag model, and the existing runtime contract occurrence shape; tune only frontier sector initialization plus colony/security/economy progression thresholds in the declared owners without reintroducing numeric economy state or expanding report UI again.
  - [ ] VERIFICATION: Success criteria/tests to run: seeded frontier guardrails pass; fixed-seed composite chronicle runs on 30, 300, and 3000 ticks show materially lower explorer failure dominance and slower, more legible frontier growth than the recorded baseline (`204` sectors, `132` bottlenecks, `persistent_nova` at `2970 expedition_failed` and `2783 move` over 3000 ticks) while preserving live trade and contract activity; focused agent/grid/integration/reasonableness tests for the touched slice stay green; current chronicle/report tooling remains unchanged as the inspection surface; touched-file diagnostics stay clean; headless `godot --path "$PWD" --no-window --quit` stays clean apart from the known GLES2 sharpening warning; defer broader military/pirate role-semantic calibration to the following milestone once frontier behavior is no longer pathological.
