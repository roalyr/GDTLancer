## CURRENT GOAL: In-Flight Control Continuity + Queued Jump Alignment
- TARGET_FILE: src/modules/piloting/player_controller_ship.gd
- TRUTH_RELIANCE: TRUTH_PROJECT.md; TRUTH_CONSTRAINTS.md §1; TRUTH_CONTENT-CREATION-MANUAL.md §4.2 "PID Controller Tuning", §6.1 "Quick Validation", §6.3 "GUT Unit Tests"
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [x] TASK_1: Extend the in-flight MainHUD controls in `src/core/ui/main_hud/main_hud.gd` so an already-active camera drag keeps rotating when the cursor crosses projected target brackets, HUD buttons, and HUD sliders. Preserve `MainHUD._instance_projected_target_bracket()`, `MainHUD._rebuild_route_target_overlay()`, `MainHUD._rebuild_world_target_overlay()`, the existing button and slider callback signatures in `main_hud.gd`, `orbit_camera.gd::set_is_rotating()`, and the current bracket-specific click-release vs drag-release semantics in `projected_target_bracket.gd`. Required behavior: drags that start in world space and then cross an in-flight Control must forward mouse motion to the live camera path and forward release back to the live player controller path without triggering unwanted button presses or slider changes. Exclude modal and debug panels.
  - [x] TASK_2: Update `player_controller_ship.gd` to queue jump alignment and auto-execute once orientation is good enough, reusing `route_target.gd::route_direction`, `agent.gd::command_align_to(direction: Vector3)`, `movement_system.gd::is_aligned_to(target_direction)`, `movement_system.gd::is_rotation_stopped()`, `Constants.DEFAULT_ALIGNMENT_ANGLE_THRESHOLD`, and the existing `player_jump_requested` signal. Required behavior: pressing interact on a selected jump route or jump target must align first when misaligned, emit `player_jump_requested` immediately only when alignment has been achieved, auto-fire the queued jump once aligned, and clear the queue on selection loss or any explicit navigation override.
  - [x] VERIFICATION: Extend focused GUT coverage in `test_main_hud_projected_targeting.gd` and `test_docking_logic.gd` for control-crossing camera drag continuity, clean drag release, no accidental button or slider activation during drag, queued jump alignment, auto-jump on alignment completion, and jump-queue cancellation on target or command changes. Then run the narrow UI slice, the docking/controller slice, and a manual Godot pass for the exact in-flight UX.
