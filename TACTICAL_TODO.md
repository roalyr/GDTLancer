<!--
PROJECT: GDTLancer
MODULE: TACTICAL_TODO.md
STATUS: [Level 2 - Implementation]
TRUTH_LINK: TRUTH_PROJECT.md § Project Stack And Context; TRUTH_PROJECT.md § Automated Testing Boundary; TRUTH_SIMULATION-GRAPH.md §0, §6.1, §6.3, §6.4; TRUTH_CONTENT-CREATION-MANUAL.md §3.4; TRUTH_CONSTRAINTS.md §1; MODEL-CASCADE-PROTOCOL.md ROLE: Lead Systems Architect
LOG_REF: 2026-05-26 19:48:00
-->

## CURRENT GOAL: Player-Facing Contract Board And Procedural Station Generation
- TARGET_SCOPE: Expose the runtime contract board to the player via StationMenu so docked players can accept one runtime contract occurrence at a time and complete it for rewards. Simultaneously, generate a dockable station automatically in each discovered sector using the existing station framework, with simple naming (Sector name + "Station"), so discovered sectors are immediately playable and non-empty. Both features reuse the existing qualitative simulation model (no numeric stockpiles or trading) and preserve the player agent as an "idle" role that claims/delivers contracts through the same cargo/dock pathway as NPC agents.
- TARGET_FILES:
  - src/core/simulation/contract_generation_system.gd — Extend runtime contract occurrences with player-facing delivery metadata (source, destination, cargo requirement, reward credits).
  - src/core/simulation/agent_layer.gd — Integrate player agent into contract claim/delivery loop; procedurally generate a dockable station per discovered sector with deterministic naming and location registration.
  - src/autoload/GameState.gd — Store runtime contract occurrences, player agent state (claimed occurrence, cargo_tag), sector/station metadata (procedural docking points, generated station ids).
  - src/core/ui/station_menu/station_menu.gd — Replace contracts/trade placeholders with a docked contract board UI that displays eligible runtime occurrences and allows player to accept one at a time.
  - scenes/ui/menus/station_menu/StationMenu.tscn — Update UI layout to show contract board with source/destination/reward labels and accept button.
  - src/autoload/Constants.gd — Extend with procedural station naming prefixes, reward tuning constants if needed.
  - src/tests/core/simulation/test_contract_generation_system.gd — Extend tests to cover player-facing contract metadata and claim/delivery retention across generator refresh.
  - src/tests/core/simulation/test_agent_layer.gd — Extend tests to cover player agent contract claim/delivery, procedural station generation per sector, and deterministic naming.
  - src/tests/core/ui/test_station_menu.gd — New: focused UI regression tests for contract board display, player acceptance, and procedural station listing (if any).
  - src/core/simulation/grid_layer.gd — Apply destination-sector relief progression hooks so completed deliveries can affect sector welfare/growth pathways.
  - src/tests/core/simulation/test_grid_layer.gd — Add focused deterministic coverage for contract-driven sector-impact gate behavior.
  - src/autoload/CharacterSystem.gd — Extend reward credit application so player contract completion integrates with existing character credit system without reopening inventory scope.
- TRUTH_RELIANCE: ["TRUTH_PROJECT.md § Project Stack And Context", "TRUTH_PROJECT.md § Automated Testing Boundary", "TRUTH_SIMULATION-GRAPH.md §0 Implementation Reality", "TRUTH_SIMULATION-GRAPH.md §6.1", "TRUTH_SIMULATION-GRAPH.md §6.3", "TRUTH_SIMULATION-GRAPH.md §6.4", "TRUTH_CONTENT-CREATION-MANUAL.md §3.4", "TRUTH_CONSTRAINTS.md §1"]
- TECHNICAL_CONSTRAINTS: ["Platform (primary): Godot3 (3.6 stable)", "Graphics: GLES2 (for performance and compatibility)", "Additional: Python3 (for sandbox)", "Godot3 constraints: @export, @onready, and await syntax are forbidden; use only Godot3-compatible GDScript patterns"]
- ATOMIC_TASKS:
  - [x] TASK_1: Extend runtime contract occurrences with stable player-facing delivery metadata (source sector id, destination sector id, required cargo_tag, reward_credits amount) so the player UI and completion logic have deterministic contract fields. Do not alter NPC contract claim/delivery semantics; only add optional player-facing fields that survive generator refresh. Required signatures: `runtime_contract_occurrence` dictionary structure gains optional `player_displayable: bool`, `source_sector_id: String`, `destination_sector_id: String`, `required_cargo_tag: String`, `reward_credits: int` keys; `contract_generation_system.gd` still produces NPC occurrences unchanged; new player-facing metadata is added during occurrence build, not after; no numeric stockpiles or prices are reintroduced; deterministic occurrence id retention across ticks is preserved.
  - [x] TASK_2: Integrate the player agent into the existing contract claim/delivery pathway so the player can claim one runtime occurrence at a time and progress through cargo/dock states just like NPC agents, without widening the player agent role or breaking existing movement/docking logic. Required signatures: player agent cargo_tag and claimed_occurrence_id are stored in `GameState.player_cargo_tag` and `GameState.player_claimed_occurrence_id`; `agent_layer.gd` includes player in the agent loop for `_best_runtime_contract_occurrence_id()` and `_action_service_contract()` but only if player is docked and explicitly accepting; player delivery resets `player_cargo_tag` and clears `player_claimed_occurrence_id` upon completion; no NPC agent behavior is altered; existing undock/movement logic stays intact.
  - [x] TASK_3: Replace the StationMenu contracts placeholder stub with a docked contract board that displays only runtime occurrences relevant to the current dock sector, allows the player to accept exactly one claimed occurrence at a time, and shows explicit availability messaging if no contracts are present. Required signatures: `station_menu.gd` no longer calls `_show_deferred_service_feedback()` for contracts; instead, it builds a contract board from `GameState.runtime_contract_occurrences` filtered by current sector as source; player can click "Accept" to claim the occurrence only if not already holding one; accepted occurrence updates `GameState.player_claimed_occurrence_id` and adds the required cargo_tag to player inventory; UI shows destination sector name, reward credits, and cargo requirement clearly; existing trade/undock buttons remain intact; no inventory/trading scope is widened.
  - [x] TASK_4: For each discovered sector registered in `GameState.discovered_sectors`, auto-generate a dockable station per procedural rules: one station per sector, deterministic naming (sector display_name + " Station"), location determined by sector region boundaries with a safe docking point, and registration into the sector's location template so it appears as a targetable/dockable location just like handcrafted stations. Required signatures: `agent_layer.gd` calls a new helper `_generate_procedural_station_for_sector(sector_id: String) -> Dictionary` during sector discovery or world initialization; returned station dict includes `id`, `display_name`, `docking_point: Vector3`, and is registered into `TemplateDatabase` and `GameState.station_by_id`; generated stations are deterministic and replayable (same seed = same stations); one station per sector only; docking/targeting logic treats procedural stations identically to handcrafted ones; no change to existing handcrafted station loading.
  - [x] TASK_5: Complete player contract delivery at the destination dock by clearing the claimed occurrence from `GameState`, resetting player cargo_tag, and applying reward credits through the existing `CharacterSystem` path so MainHUD credit refresh works without reopening inventory or trading scope. Required signatures: `agent_layer.gd` detects player docking at destination sector, calls a new helper `_complete_player_contract_delivery()`, which looks up the claimed occurrence by id, verifies the destination sector matches, deducts the cargo from player, applies reward credits via `CharacterSystem.add_credits(...)`, and clears both `player_claimed_occurrence_id` and `player_cargo_tag`; the claimed occurrence is marked complete and removed from active occurrences on next generator tick; MainHUD credit display updates automatically; no new inventory/payout UI is added; existing character credit system flow is preserved.
  - [x] TASK_6: Update StationMenu.tscn and create focused UI regression tests so contract board display and procedural station integration are validated without widening smoke scope. Required signatures: `StationMenu.tscn` adds a new `ContractBoard` container with a list view for available occurrences (source, destination, cargo, reward) and an "Accept" button per contract; scene structure preserves all existing buttons (Undock, Info label); `src/tests/core/ui/test_station_menu.gd` is a new file that instantiates the live `StationMenu.tscn`, seeds minimal sector/station/occurrence state in `GameState`, and verifies three surfaces: contract board shows only occurrences where current sector is the source, clicking "Accept" updates player state correctly, and procedural stations appear in the station listing if any are present in the current sector; no manual long-run UI validation is required; exact-file GUT run covers this suite only.
  - [x] TASK_7: Enforce player-agent parity for sector impact by applying the same qualitative destination-sector relief/progression effects on player contract completion that NPC contract completions produce, so user-driven deliveries can intentionally influence local welfare/growth trends. Required signatures: `agent_layer.gd` routes both NPC and player completion through a shared sector-impact helper (or equivalent single source of truth) and does not special-case player out of sector impact; destination-sector contract-relief state is updated deterministically on completion without introducing numeric economy simulation; player remains user-driven for action selection only, while completion consequences stay simulation-equivalent to NPCs.
  - [x] VERIFICATION: Success criteria/tests to run: `TASK_1` is validated by adding focused `test_contract_generation_system.gd` assertions for occurrence structure (player_displayable, source_sector_id, destination_sector_id, required_cargo_tag, reward_credits present and immutable across refresh); `TASK_2` is validated by extending `test_agent_layer.gd` with player contract claim/delivery state transitions; `TASK_3` is validated by focused `test_station_menu.gd` contract board display and accept flow; `TASK_4` is validated by extending `test_agent_layer.gd` with procedural station generation determinism and location registration; `TASK_5` is validated by integration test for player delivery reward application and state cleanup; `TASK_6` is validated by running exact-file `test_station_menu.gd` and verifying no new smoke loops are introduced; `TASK_7` is validated by focused `test_agent_layer.gd` + `test_grid_layer.gd` checks that player and NPC completion both trigger the same destination-sector relief/progression gate effects. Overall validation: touched-file diagnostics stay clean for all owned files; exact-file GUT runs pass on contract_generation_system, agent_layer, grid_layer, and station_menu test suites (no broad suite reintroduction); headless `godot --path "$PWD" --no-window --quit` passes with only known GLES2 sharpening warning; manual in-game validation confirms player can dock, accept one contract, travel to destination, receive credits, and observe parity-consistent sector-impact direction at delivery destinations; procedural stations are present in all discovered sectors and are dockable/targetable.
