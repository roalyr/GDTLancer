<!--
PROJECT: GDTLancer
MODULE: TACTICAL_TODO.md
STATUS: [Level 2 - Implementation]
TRUTH_LINK: TRUTH_PROJECT.md § Project Stack And Context; TRUTH_PROJECT.md § Compatibility Constraints; TRUTH_PROJECT.md § Automated Testing Boundary; TRUTH_PROJECT.md § Workflow And Scope Boundary; TRUTH_PROJECT.md § Session Logging Boundary; TRUTH_SIMULATION-GRAPH.md §0; TRUTH_SIMULATION-GRAPH.md §6.4; MODEL-CASCADE-PROTOCOL.md ROLE: Lead Systems Architect
LOG_REF: 2026-05-28 14:01:46
-->

## CURRENT GOAL: Continuous Raw Simulation Stream
- TARGET_SCOPE: Add an additive Sim Debug Panel path that starts the live simulation in an unbounded raw console-stream mode without taking over the in-game report view. Pressing the raw button must stop using focus/tick-count/report-detail filters and instead emit a full console snapshot for every processed tick until the process exits or the engine is torn down. The stream must keep the extendable JSON-lines schema, and its `game_state` payload must be sourced from the full script-defined `GameState` property list so newly added simulation fields are picked up automatically without maintaining another manual field whitelist. Existing chronicle report buttons, report formatting, bounded batch-report APIs, and composite research flow must remain intact.
- TARGET_FILES:
  - TRUTH_SIMULATION-GRAPH.md — Document the continuous raw stream, its run-start/per-tick/graceful-shutdown semantics, and the normalization rules the later parser milestone must rely on.
  - scenes/ui/hud/sim_debug_panel.tscn — Keep the raw-run control visible, but align its copy with the continuous raw-stream behavior.
  - src/core/ui/sim_debug_panel/sim_debug_panel.gd — Route the raw-run request through the continuous engine/logger seam without switching the panel into report mode or forwarding report-scoping filters.
  - src/core/simulation/simulation_engine.gd — Expose an additive public API that starts the continuous raw stream while preserving the existing chronicle report APIs and the bounded snapshot helper.
  - src/core/simulation/simulation_raw_logger.gd — Own the JSON-lines schema, continuous-run lifecycle helpers, recursive Variant normalization, `GameState` property introspection, and console emission.
  - src/tests/core/ui/test_sim_debug_panel.gd — Verify raw-run button plumbing, removal of report-scope forwarding for the raw path, and that the report window is not replaced by the raw stream.
  - src/tests/core/simulation/test_simulation_tick.gd — Verify the engine-level continuous raw-stream API starts without a tick limit and keeps advancing the live simulation after activation, while the bounded helper remains intact.
  - src/tests/core/simulation/test_simulation_raw_logger.gd — Verify schema record envelopes, continuous-run activation metadata, normalized type encoding, and automatic inclusion of real script-defined `GameState` fields.
- TRUTH_RELIANCE: ["TRUTH_PROJECT.md § Project Stack And Context", "TRUTH_PROJECT.md § Compatibility Constraints", "TRUTH_PROJECT.md § Automated Testing Boundary", "TRUTH_PROJECT.md § Workflow And Scope Boundary", "TRUTH_PROJECT.md § Session Logging Boundary", "TRUTH_SIMULATION-GRAPH.md §0", "TRUTH_SIMULATION-GRAPH.md §6.4"]
- TECHNICAL_CONSTRAINTS: ["Platform (primary): Godot3 (3.6 stable)", "Graphics: GLES2 (for performance and compatibility)", "Additional: Python3 (for sandbox)", "Forbidden GDScript syntax in this repo: `@export`, `@onready`, and `await`.", "Public signatures, initialization contracts, serialization/save-load shape, scene/resource/template loading guarantees, and other stable API or data-shape invariants must stay in GUT.", "Exact tick order, system handoff contracts, report/request plumbing, and deterministic event-packet or occurrence-dictionary structure must stay in GUT.", "Broad smoke tests, long-run world reasonableness, live balance, and UI copy/presentation iteration remain manual validation surfaces."]
- OUT_OF_SCOPE: The external Python parser, writing logs to files instead of the live console stream, replacing or removing the existing chronicle/composite report buttons, adding a new long-run GUT smoke harness, or curating hand-written per-system snapshot schemas that must be updated separately from `GameState`.
- VALIDATION_PLAN: Keep automated validation narrow and local. After each implementation slice, use touched-file diagnostics and `godot --path "$PWD" --no-window --quit`. Add or update focused GUT coverage for `test_sim_debug_panel.gd`, `test_simulation_tick.gd`, and `test_simulation_raw_logger.gd`, but do not auto-run GUT in this workflow; the user-run commands must use `-gdir=` overrides.
- MANUAL_VALIDATION: Open Sim Debug Panel, trigger the raw-run control, and confirm the live panel header stays in snapshot mode instead of switching to the chronicle report view. Confirm the console immediately emits a `run_started` JSON record, continues emitting one full `tick_snapshot` record per processed tick without a preset tick limit, and keeps running until the app/process is closed. If the app is closed gracefully, confirm a `run_finished` record is emitted; if the process is killed hard, treat the missing closing record as expected best-effort behavior. Confirm the raw request metadata does not carry focus/sort/detail filters and that known `GameState` fields such as `world_seed` and `sim_tick_count` appear in `game_state` without hand-editing the logger field list.
- ATOMIC_TASKS:
  - [x] TASK_1: Shift the panel-owned raw logger from a bounded filtered batch into a continuous unbounded stream. Required signatures: `sim_debug_panel.gd::_on_run_batch(tick_count: int) -> void` remains intact; `simulation_engine.gd::run_batch_and_report(tick_count: int, epoch_size: int = 1, report_request: Dictionary = {}) -> String` and `run_composite_research_report(tick_counts: Array, composite_request: Dictionary = {}) -> String` remain intact; keep `run_silent_simulation_log(...)` as an additive bounded helper if needed, add an additive public engine method that starts the continuous raw stream, keep the old report path unchanged, remove tick-count/report-filter forwarding from the raw button path, emit one full JSON snapshot record per processed tick, and document the continuous-stream/graceful-shutdown contract in `TRUTH_SIMULATION-GRAPH.md`.
  - [ ] VERIFICATION: Success criteria/tests to run: touched-file diagnostics are clean, `godot --path "$PWD" --no-window --quit` still boots cleanly, and the implementing agent lists exact user-run focused GUT commands for `test_sim_debug_panel.gd`, `test_simulation_tick.gd`, and `test_simulation_raw_logger.gd` using `-gdir=` overrides. Manual validation remains the authority for confirming the console log volume, real-time usability, hard-kill behavior, and downstream parser ergonomics.
