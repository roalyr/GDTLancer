<!--
PROJECT: GDTLancer
MODULE: TACTICAL_TODO.md
STATUS: [Level 2 - Implementation]
TRUTH_LINK: TRUTH_PROJECT.md § Project Stack And Context; TRUTH_PROJECT.md § Compatibility Constraints; TRUTH_PROJECT.md § Agent Parity Principle; TRUTH_PROJECT.md § Automated Testing Boundary; TRUTH_PROJECT.md § Workflow And Scope Boundary; TRUTH_PROJECT.md § Session Logging Boundary; TRUTH_SIMULATION-GRAPH.md §6.3; TRUTH_SIMULATION-GRAPH.md §6.4; TRUTH_SIMULATION-GRAPH.md §8.3; TRUTH_SIMULATION-GRAPH.md §8.4; TRUTH_SIMULATION-GRAPH.md §8.7; GDD-REVISION-LEDGER.md REV_002; GDD-REVISION-LEDGER.md REV_004; MODEL-CASCADE-PROTOCOL.md ROLE: Lead Systems Architect
LOG_REF: 2026-06-03 21:26:48
-->

## CURRENT GOAL: Lawful And Black-Market Dock Trade Activation
- TARGET_SCOPE: Activate the existing docked market service as a player-facing lawful/unlawful trade seam inside the current one-sector-one-dockable world model. Replace StationMenu's deferred trade placeholder with a minimal deterministic dock-market surface backed by the live `market_inventory`, `available_services`, player credits, and player commodity inventory. The milestone must route lawful `trade` and illicit `black_market` through the same docked authority without inventing per-commodity contraband schema, reputation systems, multi-dockable sectors, or a second contract authority.
- TARGET_FILES:
  - database/definitions/location_template.gd — preserve the authoritative compatibility contract for `market_inventory` and `available_services`, and clarify any live service-id expectations only if the dock-market seam needs it.
  - scenes/ui/menus/station_menu/StationMenu.tscn — own the minimal dock-market controls or containers needed to surface current market offers without redesigning the docked shell.
  - src/core/ui/station_menu/station_menu.gd — own docked market activation, lawful versus `black_market` routing, buy/sell mutations, service gating, and player-facing status updates while preserving the global Contract Board path.
  - src/tests/core/ui/test_station_menu.gd — deterministic coverage for lawful trade, black-market routing, unavailable-service gating, preserved Contract Board ownership, and docked-shell state updates.
  - src/tests/core/ui/test_debug_window.gd — replace the stale deferred-trade expectation with the new StationMenu market behavior without widening debug-window implementation scope.
  - src/tests/autoload/test_game_state_manager.gd — verify that dock-trade mutations to location market quantities plus player credits or commodity inventory survive save/load if the live service now mutates them.
- TRUTH_RELIANCE: ["TRUTH_PROJECT.md § Project Stack And Context", "TRUTH_PROJECT.md § Compatibility Constraints", "TRUTH_PROJECT.md § Agent Parity Principle", "TRUTH_PROJECT.md § Automated Testing Boundary", "TRUTH_PROJECT.md § Workflow And Scope Boundary", "TRUTH_PROJECT.md § Session Logging Boundary", "TRUTH_SIMULATION-GRAPH.md §6.3", "TRUTH_SIMULATION-GRAPH.md §6.4", "TRUTH_SIMULATION-GRAPH.md §8.3", "TRUTH_SIMULATION-GRAPH.md §8.4", "TRUTH_SIMULATION-GRAPH.md §8.7", "GDD-REVISION-LEDGER.md REV_002", "GDD-REVISION-LEDGER.md REV_004"]
- TECHNICAL_CONSTRAINTS: ["Platform (primary): Godot3 (3.6 stable)", "Graphics: GLES2 (for performance and compatibility)", "Additional: Python3 (for sandbox)", "Forbidden GDScript syntax in this repo: `@export`, `@onready`, and `await`.", "The player is a first-class simulation agent under the same fundamental world contracts as NPC agents.", "Public signatures, initialization contracts, serialization/save-load shape, scene/resource/template loading guarantees, and other stable API or data-shape invariants must stay in GUT.", "UI-focused GUT tests should assert behavior/state transitions (signals, visibility, enabled/disabled state, data plumbing, and interaction outcomes) instead of literal label text, unless that text is explicitly required by a truth-level contract.", "Broad smoke tests whose primary claim is only that \"the live simulation ran for N ticks\" without protecting a specific contract must stay manual.", "If the required change widens behavior outside `TARGET_SCOPE` or requires non-narrow owners outside `TARGET_FILES`, return control to the Architect instead of improvising scope."]
- OUT_OF_SCOPE: NPC trade-affinity retuning, generic agent-to-agent market rewrites, per-commodity contraband or legality fields, faction standings or reputation UI, price rebalance across the live location registry, Contract Board ownership changes, multi-dockable sector work, topology changes, and station-menu art or layout redesign beyond the minimal controls needed to operate the dock market.
- PREAPPROVED_ADJACENT_OWNERS:
  - SESSION-LOG.md — required reverse-chronological state log for architect, developer, and verificator entries.
  - src/core/systems/character_system.gd — allowed only if a minimal additive helper is required to keep player credit mutations localized to the existing owner.
  - src/core/systems/inventory_system.gd — allowed only if a minimal additive helper is required to keep player commodity add/remove logic localized to the existing owner.
  - src/autoload/GameStateManager.gd — allowed only if a narrow persistence compatibility fix is required for dock-market mutations that should already survive save/load.
  - src/core/simulation/affinity_matrix.gd — allowed only if a small shared helper is required so the dock-market path uses the same lawful/illicit vocabulary already defined by the live interaction layer.
- VALIDATION_PLAN: Keep executable validation narrow. After each implementation slice, use touched-file diagnostics plus `godot --path "$PWD" --no-window --quit`. Add or update focused deterministic coverage in `test_station_menu.gd` and `test_debug_window.gd`, and touch `test_game_state_manager.gd` only if dock-market mutations expose a persistence seam. Do not auto-run GUT in this workflow; the implementing agent must list exact user-run `-gdir=` commands for every touched test owner.
- MANUAL_VALIDATION: Dock at a lawful service hub such as Elace and confirm the dock-market surface can buy and sell one commodity against live credits and market quantities without affecting the global Contract Board path. Dock at a lawless or black-market hub such as Vidr and confirm unlawful-market access is surfaced distinctly when `black_market` is offered, still within the current one-sector-one-dockable shell. Save and reload after one dock trade to confirm the live market/inventory state reads coherently in the UI.
- ATOMIC_TASKS:
  - [ ] TASK_1: Replace StationMenu's deferred trade placeholder with a minimal dock-market surface that reads the current dock's `market_inventory` and the player's current credits or commodity holdings without changing the global Contract Board flow. Required signatures: `open_for_current_dock()`, `_on_trade_pressed() -> void`, `_on_contracts_pressed() -> void`, and `_location_offers_service(location_id: String, service_id: String) -> bool` remain intact.
  - [ ] TASK_2: Route lawful `trade` and illicit `black_market` through the same docked market authority using existing `available_services` plus live dock legality context, without introducing a new commodity legality schema or a separate dock authority. Required signatures: `LocationTemplate.market_inventory` remains `{commodity_template_id: {buy_price: int, sell_price: int, quantity: int}}`, `available_services` remains the authoritative service list, and the global Contract Board remains the only player contract action owner.
  - [ ] TASK_3: Apply buy/sell mutations through the existing player credit and inventory owners while preserving compatibility with save/load and the current qualitative cargo mirrors. Required signatures: `CharacterSystem.add_credits(...)`, `CharacterSystem.subtract_credits(...)`, `InventorySystem.add_asset(...)`, `InventorySystem.remove_asset(...)`, and the current `GameStateManager` character/inventory/location persistence shape remain intact; `player_claimed_occurrence_id` and protected runtime contract cargo remain outside the dock-market mutation path.
  - [ ] TASK_4: Add or update focused deterministic coverage for lawful market exchange, black-market routing, unavailable-service gating, preserved Contract Board behavior, and any save/load persistence touched by dock-market mutations.
  - [ ] VERIFICATION: Success criteria/tests to run: touched-file diagnostics are clean, `godot --path "$PWD" --no-window --quit` still boots cleanly, the implementing agent lists exact user-run focused GUT commands with `-gdir=` overrides for every touched test owner, and the final note states explicitly that per-commodity contraband rules, faction reputation, broader NPC market behavior, and long-run market balance remain out of scope or manual for this milestone.
