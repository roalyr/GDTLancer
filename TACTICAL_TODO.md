## CURRENT GOAL: Jump Transition Constant Canonicalization
- TARGET_FILE: src/autoload/Constants.gd
- TRUTH_RELIANCE: TRUTH_PROJECT.md (Godot3/GLES2/Python3 stack); TRUTH_CONSTRAINTS.md §1; TRUTH_CONTENT-CREATION-MANUAL.md §2 and §7; TRUTH_SIMULATION-GRAPH.md §1 and §3.2/§3.3 as refactor boundaries so jump travel semantics stay unchanged while implementation constants are centralized and cleaned up.
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [ ] TASK_1: Create a dedicated jump-transition section in `src/autoload/Constants.gd` and move every shared jump-transition tuning value and identifier there without changing the current jump flow. Required signatures: preserve `world_manager.gd::func travel_to_sector(target_sector_id: String) -> void`, `world_manager.gd::func load_sector(sector_id: String)`, `world_manager.gd::func is_jump_transition_active() -> bool`, `jump_transition_rig.gd::func begin_departure(source_sector_id: String, target_sector_id: String, travel_direction: Vector3) -> void`, `jump_transition_rig.gd::func begin_cruise(target_velocity: float) -> void`, and `jump_transition_rig.gd::func begin_arrival() -> void`. Required constant scope: rig node name, default travel direction, minimum cruise speed, FoV target/durations/easing power, camera-aim delay, HUD show delay, velocity tolerances/timeouts, route timing values, acceleration/deceleration, and route-completion tolerance must resolve through `Constants`.
  - [ ] TASK_2: Refactor `world_manager.gd`, `jump_transition_rig.gd`, and the focused jump-transition regressions to consume the autoloaded constants as the only source of truth; remove duplicate local constant blocks, stale names, and any dead jump-transition helpers or branches left unused after the migration. Required signatures: preserve `world_manager.gd::func _prepare_jump_transition_departure_visuals(departure_direction: Vector3)`, `world_manager.gd::func _get_jump_transition_cruise_speed(route_distance: float) -> float`, `world_manager.gd::func _get_jump_transition_route_timeout_sec(route_distance: float, cruise_speed: float) -> float`, `jump_transition_rig.gd::func reset_transition_state() -> void`, `jump_transition_rig.gd::func get_current_velocity() -> float`, `jump_transition_rig.gd::func is_route_complete() -> bool`, and `jump_transition_rig.gd::func get_transition_camera_forward_direction() -> Vector3`. Required cleanup result: no jump-transition tuning constants remain declared in `world_manager.gd` or `jump_transition_rig.gd`, and focused tests reference `Constants` rather than script-local values.
  - [ ] VERIFICATION: Run focused diagnostics on `src/autoload/Constants.gd`, `src/scenes/game_world/world_manager.gd`, and `src/scenes/game_world/jump_transition/jump_transition_rig.gd`; run the narrow GUT slices for `src/tests/scenes/game_world/world_manager/test_world_manager.gd`, `src/tests/scenes/jump_transition/test_jump_transition_regressions.gd`, and `src/tests/scenes/camera/test_orbit_camera.gd`; run `godot --no-window --quit`. Success criteria: all touched files are error-free, focused tests pass, jump-transition behavior matches the current tuned flow, and no duplicate local jump-transition constant declarations remain outside `Constants.gd`.
