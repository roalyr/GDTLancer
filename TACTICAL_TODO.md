<!--
PROJECT: GDTLancer
MODULE: TACTICAL_TODO.md
STATUS: [Level 2 - Implementation]
TRUTH_LINK: TRUTH_PROJECT.md § Project Stack and Context; TRUTH_SIMULATION-GRAPH.md §0, §2.3, §6.1, §6.3, §6.4; TRUTH_CONTENT-CREATION-MANUAL.md §3.4, §4.1, §4.3, §6; TRUTH_CONSTRAINTS.md §1; MODEL-CASCADE-PROTOCOL.md ROLE: Lead Systems Architect
LOG_REF: 2026-05-26 10:04:52
-->

## CURRENT GOAL: Hub Saturation Rebalance And Discovery Name Legibility
- TARGET_SCOPE: Treat the manual focused/composite chronicle logs as the fixed inspection surface and retune the live qualitative late-cycle world so colonies and hubs still emerge organically but no longer saturate the map during prosperity/recovery normalization waves, while runtime-discovered sectors keep unique human-readable display names throughout long runs. Scope covers recovery-sensitive colony/hub progression, late-cycle maturity pacing, and deterministic discovered-sector name uniqueness. It does not reintroduce hard maturity caps, alter topology/discovery connection selection again, widen chronicle/report UI, or restore an automated 300-3000 tick full-environment GUT harness.
- TARGET_FILES:
  - src/autoload/Constants.gd — central owner for recovery/prosperity maturity thresholds and any discovery-name uniqueness tuning constants.
  - src/core/simulation/grid_layer.gd — owner for outpost/colony/hub progression and the recovery-era promotion wave that currently overconverts the world into hubs.
  - src/core/simulation/agent_layer.gd — owner for deterministic discovered-sector name generation and runtime discovered-sector registration.
  - src/tests/core/simulation/test_grid_layer.gd — focused guardrails for colony-to-hub pacing and recovery-sensitive maturity behavior.
  - src/tests/core/simulation/test_agent_layer.gd — focused guardrails for deterministic, unique discovery-name generation.
  - src/tests/core/simulation/test_simulation_integration.gd — short seeded integration coverage for the touched maturity/naming slice without reintroducing a long-run harness.
- TRUTH_RELIANCE: ["TRUTH_PROJECT.md § Project Stack and Context", "TRUTH_SIMULATION-GRAPH.md §0 Implementation Reality", "TRUTH_SIMULATION-GRAPH.md §2.3 The Agent Nodes", "TRUTH_SIMULATION-GRAPH.md §6.1", "TRUTH_SIMULATION-GRAPH.md §6.3", "TRUTH_SIMULATION-GRAPH.md §6.4", "TRUTH_CONTENT-CREATION-MANUAL.md §3.4", "TRUTH_CONTENT-CREATION-MANUAL.md §4.1", "TRUTH_CONTENT-CREATION-MANUAL.md §4.3", "TRUTH_CONTENT-CREATION-MANUAL.md §6", "TRUTH_CONSTRAINTS.md §1"]
- TECHNICAL_CONSTRAINTS: ["Platform (primary): Godot3 (3.6 stable)", "Graphics: GLES2 (for performance and compatibility)", "Additional: Python3 (for sandbox)"]
- ATOMIC_TASKS:
  - [x] TASK_1: Rebalance late-cycle sector maturation so `colony -> hub` remains possible but no longer dominates the world mix during prosperity/recovery normalization waves. Required signatures: preserve `GridLayer.process_tick(config: Dictionary) -> void`, `SimulationEngine.initialize_simulation(seed_string: String) -> void`, `SimulationEngine.process_tick() -> void`, and the qualitative sector-tag model; tune only age-sensitive promotion thresholds, colony/hub gating, and narrow supporting constants within the declared owners.
  - [x] TASK_2: Make runtime-discovered sector display names deterministic and unique across long runs without changing discovered sector ids or the chronicle event packet shape. Required signatures: preserve `AgentLayer.process_tick(config: Dictionary) -> void`, `AgentLayer._generate_sector_name_for_count(discovery_count: int) -> String`, discovered ids of the form `discovered_N`, and the chronicle event packet shape `{tick, actor_id, action, sector_id, metadata}`; adjust only the naming algorithm and its supporting constants within the declared owners.
  - [x] TASK_3: Add focused short guardrails for recovery-era hub pacing and duplicate-name prevention without reintroducing a long-run automated world harness. Required signatures: preserve the current focused/composite chronicle APIs and keep full-environment 300-3000 tick validation manual-only; update only the declared short grid/agent/integration tests needed to lock the rebalance.
  - [x] VERIFICATION: Success criteria/tests to run: manual focused/composite 300 and 3000 tick logs still show active exploration, trade, relief-contract claim/delivery flow, and bounded topology, but no longer end in the current hub-saturation pattern (`16 hubs / 19 sectors` at 300 and `152 hubs / 191 sectors` at 3000); colonies/outposts remain materially represented after long runs; discovered-sector display names stay unique in both discovery summaries and final world summaries; focused grid/agent/short integration tests for the touched slice stay green; no automated long-run full-environment harness is required; touched-file diagnostics stay clean; headless `godot --path "$PWD" --no-window --quit` stays clean apart from the known GLES2 sharpening warning.
