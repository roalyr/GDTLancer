## CURRENT GOAL: TransitionOverlay Timed Envelope and Curved Fade
- TARGET_FILE: src/scenes/game_world/jump_transition/jump_transition_rig.gd
- TRUTH_RELIANCE: TRUTH_PROJECT.md; TRUTH_CONSTRAINTS.md §1; TRUTH_CONTENT-CREATION-MANUAL.md §2, §4, §6, §7; TRUTH_DOCS_CanvasItem_Godot_3.6.md §Render modes and §Global built-ins; TRUTH_DOCS_Shading_language_Godot_3.6.md §Introduction as the ceiling if a CanvasItem shader becomes strictly necessary; TRUTH_DOCS_Spatial_shaders_Godot_3.6.md §Render modes as a negative boundary that this overlay remains a CanvasItem path, not a Spatial one; TRUTH_DOCS_Particle shaders_Godot_3.6.md note before §Render modes as a reminder that this milestone must not reopen GLES3-only particle shader work; TRUTH_SIMULATION-GRAPH.md §1 as a boundary that this pass is cosmetic-only and must not alter sector-travel or simulation semantics.
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [x] TASK_1: Replace the rig's binary TransitionOverlay toggle with a rig-owned timed envelope and canonical overlay tuning. Required signatures: preserve `jump_transition_rig.gd::func begin_departure(source_sector_id: String, target_sector_id: String, travel_direction: Vector3) -> void`, `jump_transition_rig.gd::func begin_cruise(target_velocity: float) -> void`, `jump_transition_rig.gd::func begin_arrival() -> void`, `jump_transition_rig.gd::func reset_transition_state() -> void`, and `jump_transition_rig.gd::func _set_transition_overlay_active(is_active: bool) -> void`; add stable runtime hooks `jump_transition_rig.gd::func begin_departure_overlay_window() -> void`, `jump_transition_rig.gd::func on_departure_cruise_entered() -> void`, `jump_transition_rig.gd::func begin_arrival_overlay_window() -> void`, `jump_transition_rig.gd::func on_arrival_fov_stabilized() -> void`, and `jump_transition_rig.gd::func get_transition_overlay_alpha() -> float`. Required tuning source: add the overlay envelope constants only in `src/autoload/Constants.gd`, including peak alpha, a default power-curve tuning constrained to the `0.5..0.75` band, a dedicated arrival fade-in curve and duration, `JUMP_TRANSITION_OVERLAY_POST_DEPARTURE_HOLD_SEC`, and `JUMP_TRANSITION_OVERLAY_ARRIVAL_POST_FULL_OPACITY_HOLD_SEC`. Required scene contract: keep `TransitionOverlayLayer/TransitionOverlay` owned by `scenes/prefabs/navigation/jump_transition_rig.tscn`, keep it GLES2-safe as a `ColorRect`/CanvasItem path, and drive alpha through script-visible modulation or color alpha unless a CanvasItem shader is strictly necessary.
  - [x] TASK_2: Rewire jump orchestration so the overlay envelope starts on the first FoV-shift frame and persists through the requested post-phase holds on both departure and arrival without disturbing the already verified particle split. Required signatures: preserve `world_manager.gd::func travel_to_sector(target_sector_id: String) -> void`, `world_manager.gd::func _run_jump_transition_sequence(target_sector_id: String) -> void`, `world_manager.gd::func _prepare_jump_transition_departure_visuals(departure_direction: Vector3)`, `world_manager.gd::func _restore_gameplay_camera_at_transition_fov() -> void`, `world_manager.gd::func _animate_main_camera_fov_override(target_fov: float, duration_sec: float)`, `world_manager.gd::func _animate_main_camera_fov_restore(duration_sec: float)`, `orbit_camera.gd::func set_local_scene_particles_active(is_active: bool, clear_existing: bool = false) -> void`, and `jump_transition_rig.gd::func set_transition_particles_active(is_active: bool, clear_existing: bool = false) -> void`. Required behavior: departure overlay begins exactly when the FoV widen starts, remains active through transition-camera cruise entry, and only clears after the canonical post-departure hold. Arrival overlay begins exactly when decel starts, remains active through cruise exit, player/camera handoff, and FoV restore, reaches full opacity before destination reveal, and only begins its release countdown once full opacity is reached; the configured arrival hold is a true full-opacity hold before decay begins. Existing route completion, pause/resume flow, and particle isolation must remain behaviorally unchanged.
  - [x] VERIFICATION: Confirm focused diagnostics on `src/autoload/Constants.gd`, `src/scenes/game_world/jump_transition/jump_transition_rig.gd`, `src/scenes/game_world/world_manager.gd`, and `scenes/prefabs/navigation/jump_transition_rig.tscn`; confirm focused regression coverage in `src/tests/scenes/jump_transition/test_jump_transition_regressions.gd` for departure-envelope start timing, post-departure hold extension, arrival-envelope start timing, dedicated arrival fade-in timing, full-opacity hold semantics, curved alpha progression, and reset cleanup across two consecutive jumps; confirm `godot --no-window --quit` is green. Success criteria: the overlay starts with the first FoV-widen frame on departure and the first decel frame on arrival, its alpha follows the canonical curved envelope, the departure window persists for about `2.0` seconds after cruise entry, the arrival window reaches full opacity before reveal and then holds for about `2.0` seconds before decay, and no stale overlay visibility or alpha state carries into the next jump. Per current session policy, automated verification must not invoke GUT; the final gameplay timing pass is manual in-editor verification.
