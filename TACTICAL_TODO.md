## CURRENT GOAL: Scene-Driven UI Unification
- TARGET_FILE: scenes/ui/hud/main_hud.tscn
- TRUTH_RELIANCE: TRUTH_PROJECT.md; TRUTH_CONSTRAINTS.md §1; TRUTH_CONTENT-CREATION-MANUAL.md §1 "Overview", §2 "Directory Structure Reference", §6 "Testing Your Content"; SESSION-LOG.md entries 2026-05-13 16:00:22 through 2026-05-13 16:47:02
- TECHNICAL_CONSTRAINTS:
  - Platform (primary): Godot3 (3.6 stable)
  - Graphics: GLES2 (for performance and compatibility)
  - Additional: Python3 (for sandbox)
- ATOMIC_TASKS:
  - [x] TASK_1: Rework the live UI scene graph around MainHUD conventions. Preserve `MainHUD._on_ButtonMenu_pressed()`, `MainHUD._on_ButtonDock_pressed()`, `MainMenu._show_menu()`, `SimDebugPanel._toggle()`, and `DebugMapPanel._toggle_panel()` as the stable integration points; add only the minimal new public handlers needed for `ButtonDebug` launch, shared close-button flow, and docked-window reopen. Required behavior: `ButtonDebug` opens a dedicated DebugWindow; no debug buttons or debug labels remain on MainHUD; MainMenu, StationMenu, SimDebugPanel, and DebugMapPanel use the shared 150x150 icon-only close control; MainMenu `ButtonClose` dismisses the menu and unpauses gameplay when a live session exists; docking close hides the station window while keeping the player docked; the HUD dock entry can reopen the docking window while still docked.
  - [x] TASK_2: Convert projected target UI to fully scene-authored controls. Preserve `MainHUD._instance_projected_target_bracket()`, `MainHUD._rebuild_route_target_overlay()`, `MainHUD._rebuild_world_target_overlay()`, `ProjectedTargetBracket.configure_target(new_target_ref, new_target_label: String = "")`, `ProjectedTargetBracket.set_selected_state(is_selected: bool)`, and the existing drag passthrough semantics. Required behavior: `projected_target_bracket.tscn` owns `bracket_targeting.png`, `bracket_targeting_selected.png`, and a placeholder label under the bracket via `CenteredGrowingLabel`; target, dock, and jump info is shown under projected brackets; old top-center docking, target, sector, and radar nodes are removed from active HUD use; touched labels inherit from `main_theme.tres` instead of custom font overrides.
  - [ ] VERIFICATION: Add focused GUT coverage for `ButtonDebug` launch, DebugWindow launcher routing, MainMenu close/unpause, StationMenu close/hide/reopen, bracket-owned labels, selected overlay visibility, and removal of old HUD target/prompt usage. Run touched-file diagnostics, `godot --no-window -s addons/gut/gut_cmdln.gd -gdir=res://src/tests/core/ui/ -ginclude_subdirs=false -gexit`, the docking/controller slice covering `test_docking_logic.gd` with `-gdir=` override, and a manual Godot pass for clean MainHUD, debug window flow, docked close/reopen, and projected target label behavior.
